# Test-only dependencies (CI installs this next to requirements.txt; the
# droplet does not). pip install -r requirements.txt -r requirements-dev.txt
httpx==0.28.1        # fastapi.testclient (tests/test_server_api.py)
aiohttp==3.14.1      # tools/fake_snes.py, the stand-in usb2snes bridge
